//declaracion de una funcion

function sayHello(name) {
  //creacion de una cosntante
  const greeting = "Hello, ${name}";

  //devolver constante
  return greeting;

  ///////////////////////////////

  //Codigo del profesor
  function sayHello(name) {
    const greeting = `Hello, ${Name}!`;
    return greeting;

    const helloChris = sayHello("Chris");

    console.log(helloChris);
  }
}
